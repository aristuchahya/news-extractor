# Project runtime dependencies (mirrors pyproject.toml [project].dependencies).
# Used by the Airflow Docker image to layer project deps on top of the base image.
httpx[http2]>=0.28.0
beautifulsoup4>=4.12.3
trafilatura>=1.12.2
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
lxml>=5.3.0
